import 'package:flutter/material.dart';


class Forwarded extends StatefulWidget {
  const Forwarded({Key? key}) : super(key: key);

  @override
  State<Forwarded> createState() => _ForwardedState();
}

class _ForwardedState extends State<Forwarded> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
    
  }
}